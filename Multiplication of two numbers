#include<stdio.h>
#include<conio.h>
int main()
{
	  int a,b,multiply;
	  printf("Enter the value of a = ");
    scanf("%d",&a);
    printf("Enter the value of b = ");
    scanf("%d",&b);
    multiply=a*b;
    printf("a*b =%d",multiply);
	return 0;
}
