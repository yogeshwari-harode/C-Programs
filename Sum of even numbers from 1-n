#include<stdio.h>
#include<conio.h>
int main()
{
    int n,i,sum=0;
    
    printf("Enter the value of n : ");
    scanf("%d",&n);
    
    for(i=2; i<=n; i=i+2)
    {
        printf("%d\n",i);
        sum =sum + i;
    }
    printf("Sum of Even numbers from 1-%d : %d",n,sum);
    
return 0;
}
